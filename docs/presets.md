# Panel presets

A preset is a reusable starting point for renderer/layout defaults.

Use presets to keep a dashboard consistent without copying widget JSON by hand.

Preset files validate with the same CLI:

```bash
ldw validate examples/presets
```
